# Database Configuration
# Format: postgresql://username:password@host:port/database
# Example: postgresql://postgres:postgres@localhost:5432/barber_db
# Production: Gunakan connection string dari provider (Supabase, Railway, Neon, dll)
DATABASE_URL=postgresql://username:password@localhost:5432/barber_db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Application Configuration
NODE_ENV=development
PORT=3000

# Cloud Storage Configuration (Optional - untuk invoice storage)
# Jika tidak diset, invoice akan disimpan sebagai base64 di database
CLOUD_STORAGE_URL=
CLOUD_STORAGE_API_KEY=
